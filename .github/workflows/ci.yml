name: ci

on:
  push:
    branches:
      - main
  schedule:
    - cron: "34 1,4,13,22 * * *"
  workflow_dispatch:

jobs:
  autogreen:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Clone repository
        uses: actions/checkout@v4

      - name: Set up Git configuration
        run: |
          git config --local user.email "k134577439084@outlook.com"
          git config --local user.name "Sun-Siyuan084"

      - name: Make changes
        run: |
          echo "Changes made at $(date)" >> changes.txt
          git add changes.txt

      - name: Commit and push changes
        run: |
          git commit -m "update changes.txt"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
